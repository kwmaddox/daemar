[package]
name = "board"
version = "0.1.0"
edition = "2021"
description = "The board: slip faces in bays, progressive disclosure to the raw ledger."

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["full"] }
tokio-stream = "0.1"
ledger = { path = "../../crates/ledger" }
serde_json = "1"
