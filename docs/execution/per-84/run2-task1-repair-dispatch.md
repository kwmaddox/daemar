# PER-84 Task1 bounded conventions repair

Fresh Luna executor. Read AGENTS.md, conventions.md C13/C14 (and all other C-IDs remain binding), docs/execution/per-84/active-dispatch.md, current console.rs/error.rs, and run2-task1-handoff.md. Scope ONLY the two adjudicated conventions defects below, no independent review or scope expansion. All code/tests are otherwise passing43 library tests; external missing-serve cases remain approved intermediate57pass62fail. Root independently reproduced full gate and saved docs/execution/per-84/run2-task1-root-gate.txt.

C13: console.rs publish_startup currently calls startup.to_json() before serializing to an existing io::Write, allocating intermediate strings via url()/source.to_string(). Stream publication through a PRIVATE serialization path without that owned Value/string preparation. Keep public to_json()/url() API and their standalone behavior unchanged; do not add public Serialize impl or public helper beyond skeleton. Preserve complete one-line JSON, correct escaping, exact tests, flush, partial writes and error causes. New private serialization can reuse ConfigSource Display as single spelling. No invented public API, no manual unsafe JSON string escaping, no tests weakened to accept changed semantics.

C14: repeated numeric port fixture7331 at console.rs address/default tests and error.rs bind test must be named with rationale. A shared crate-private cfg(test) fixture constant in an owned existing module may be used, avoiding new public contract. Keep independently expected wire strings and default-port assertion meaningful; don't replace the independent expected fixture with production DEFAULT_PORT or make an assertion self-equality. Numeric occurrences within same expression don't each require separate changes. No general fixture cleanup.

Writes limited to crates/daemar-card/src/console.rs, crates/daemar-card/src/error.rs and docs/execution/per-84/run2-task1-repair-* evidence + run2-task1-repair-handoff.md. Unit test mechanics may adapt to fixture naming but all asserted behavior remains. No other files/dependencies/policy or accepted external tests. Conventions repair is behavior-preserving; run existing tests before and after, do not manufacture new red for already-correct behavior. Add meaningful tests first only if a previously unmet behavior is discovered within existing scope. Save full command outputs and exact changed-file hashes. Use apply_patch for edits and documented sandbox escalation for sockets/advisory DB.

Acceptance: cargo test --locked -p daemar-card --lib (43 or more tests, zero failures); strict Clippy; just check with only named missing-serve failures; git diff --check. Include explicit C13/C14 correction sites and C1–C16 impact checklist. Fix ordinary compile/lint issues in owned code and rerun; no per-test root approval. Stop at handoff or genuine missing-authority conflict, not routine gate errors. Card escalation only via existing CLI explicit --db /Users/kendall/.daemar/daemar.db, Card01a0693e-bc16-7272-9ce9-a20f3b07f875, decision summary/reason, producer implementer/kindagent, scoped permission if needed. No completion event, reviews, extra agents, commits or Task2. Root independently validates correction and dispatches Task2 when accepted.

