# PER-84 Task1 restart checkpoint B — foundation tests only

Fresh Luna executor. Task1 code restart was authorized at63, restored64; initial meaningful listener test is present. New governing ruling66: meaningful missing-approved-API compile-red is valid initial TDD red; no artificial assert-false or failing stubs. Product scope unchanged. Before implementation proceeds, complete Task1's named foundation tests as an executable contract checkpoint, preserving the real existing listener test.

Factory inputs: current AGENTS.md, conventions.md, CONTEXT.md; docs/execution/per-84/contract.md; amended docs/plans/per-84-luna-execution-plan.md shared rules and Task1; docs/execution/per-84/task-1-restart-a-handoff.md and its saved test output; governing ruling below. No previous failed code/archive, conversations, memories, old plans or review discussions. Task1 named repository files and behavior helper contracts may be read. Read TDD skill /Users/kendall/.agents/skills/tdd/SKILL.md and its required references; plan supplies user-confirmed seams.

Ruling66: A test through an approved absent API may establish initial red by missing-API compilation failure, honestly labelled compile-red. No behavioral red is required before API existence. Once compilable, tests target actual unmet behavior. If a later case is already satisfied by a prior legitimately implemented slice, record its initial pass and preceding red/green provenance honestly; never break working behavior to manufacture red. Tests precede their corresponding bodies, exact test-source snapshot and command/output/exit plus green rerun are required. Compiler errors unrelated to missing approved API, environmental failures and artificial assertion failures are not red evidence of behavior. Append unanswered genuine scope/API questions and stop.

Implement TESTS ONLY at this checkpoint. All Task1 named subtle cases must be meaningful at the public seams: listener_zero_reports_bound_port; occupied_port_returns_bind_error_without_retry; startup_uses_bound_listener; publish_writes_one_line_and_flushes; publish_handles_partial_writes; publish_write_failure_preserves_source; publish_flush_failure_preserves_source; publish_startup_error_category_and_display. Include focused Task1 acceptance tests for both-family address formatting, ConfigSource's four spellings, full startup JSON fields/URL, domain display and new category/source cases. The assertions must prove the named behavior: e.g. exact one-line parsed startup equality plus observable flush, complete output despite partial writes, preserved I/O failure cause, not just a suffix or source-is-some. Use real loopback listener for public bind/startup; deterministic test-only io::Write boundary fakes are appropriate. Tests obey approved API exactly; no new public test methods. Test names and source are your responsibility, not supplied implementation code. Independent expected values come from supplied contracts. No producer data or real factory DB fixture changes.

Allowed writes: crate-local cfg(test) blocks in crates/daemar-card/src/console.rs, domain.rs, error.rs and minimal lib.rs module wiring if required; docs/execution/per-84/task-1-restart-b-* evidence and handoff. Do NOT add implementation bodies, declarations merely to make tests compile, dependencies, policy changes, or edit existing external tests/browser/migrations. Stop after real tests plus saved focused red/compile-red evidence; root validates this checkpoint before a different fresh executor implements. Commands: cargo test --locked -p daemar-card --lib <named test> as appropriate; record exact command, cwd, exit, stdout/stderr and source hashes. All needed core dependencies already available; don't add HTTP dependencies here. No claims a named test ran if compilation prevented it. Record compiler diagnostics precisely.

Card: 01a0693e-bc16-7272-9ce9-a20f3b07f875 at /Users/kendall/.daemar/daemar.db; CLI /Users/kendall/code/github/daemar/target/debug/card. Escalation is executor-authored decision, producer implementer, kind agent, JSON summary/reason only, explicit --db and sandbox require_escalated if necessary. No other Card writes, completion stage-event, reviews, other agents, commits or further tasks. Output docs/execution/per-84/task-1-restart-b-handoff.md. Root independently validates checkpoint and records accepted evidence; no code hints injected after dispatch.

