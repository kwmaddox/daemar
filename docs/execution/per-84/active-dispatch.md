# PER-84 active execution dispatch

This is the sole active dispatch for the resumed implementation run; prior task-1 checkpoint dispatches are historical, not instructions. It does not replace the settled product contract or approved four-task plan. Operator has authorized reaching the independent-review boundary, with all repository/browser/conventions checks complete first. Only fresh gpt-5.6-luna agents write implementation or fixes. Root orchestrates, verifies and judges; no root implementation. One code writer at a time.

## Factory inputs

Read AGENTS.md, conventions.md C1–C16 and CONTEXT.md. Read docs/execution/per-84/contract.md (approved Card38/45/46); docs/plans/per-84-luna-execution-plan.md (current corrected plan, governing Card66/67); this dispatch; the source/test files named by your assigned task; and the predecessor handoff named by root at dispatch. Do not read earlier execution plans, failed-attempt source/archive, chat/session summaries, memories or unrelated historical reviews. Existing accepted behavior spec crates/daemar-card/tests/features/console.feature plus its harness and browser/tests/console.spec.ts remain protected and authoritative. External ticket is not accessible; do not claim it was inspected.

Bindings: Card01a0693e-bc16-7272-9ce9-a20f3b07f875; authoritative DB /Users/kendall/.daemar/daemar.db; CLI /Users/kendall/code/github/daemar/target/debug/card. Base HEAD4c809b31264f6c147563c52604d6aea598487ccd, pre-existing dirty acceptance suite must remain intact. BSD-3-Clause allowance for matchit was explicitly authorized at61 and already lives in deny.toml; no further policy weakening permitted.

## Execution rule

Carry the assigned plan task through its implementation and in-scope repair loop. Do not stop merely because a meaningful test is red: missing-approved-API compile-red permits implementing that API. For new behavior on compilable APIs, use genuine tests first and then implement. An initially passing later case stays passing; cite prior legitimate red/green provenance rather than inventing a red. Artificial assertion failures, deliberately breaking correct code, environmental errors mislabelled behavioral, or reconstructed historical evidence are forbidden. Save actual command/cwd/exit/output and test-source snapshots before bodies and green reruns. Read TDD skill and references; approved plan is the already-confirmed test seam selection and overrides generic skill guidance where its private seams are explicit.

There are no additional per-test orchestration approval checkpoints. Make ordinary implementation choices within approved private scope and continue until task acceptance, or a real scope/authority/input conflict. Fix your compiler, fmt, lint, test and conventions defects in the same task. Gate-driven mechanical corrections to new crate-local tests (imports/format/io::Error::other etc.) may preserve semantics; never weaken assertions or change accepted external tests to fit code.

Protected: external tests/browser fixtures, migrations, public surface beyond approved skeleton, dependency/gate policy (except existing authorized BSD addition), scripts/check.sh/rules/lints, original user changes, unrelated files. No commits, deployments or scope cuts. Maintain settled contracts, error mappings, data/security boundaries, same-listener startup flow and existing CLI behavior. Do not implement later tasks prematurely; Task1 is foundation, Task2 Reader, Task3 HTTP/templates, Task4 CLI and acceptance.

Genuine unanswered record/API/policy conflicts: executor appends a decision using only JSON summary/reason, --producer implementer --producer-kind agent, explicit --db and Card ID, then stops. Request sandbox require_escalated when needed for that append or loopback/advisory/cache/network gates. Infrastructure permission errors are not product failures and not grounds for waiving checks. If append fails preserve stderr and report no returned sequence. After a Card answer, a fresh executor resumes. Do not ask for chat hints or use side-channel guidance.

## Verification and handoff

Run the assigned plan's acceptance commands, including cargo test --locked -p daemar-card --lib, strict Clippy and just check. Tasks1–3 may retain named S3 absent-serve failures only as specified by plan; predecessor tests and all completed-task behavior must pass. No fixed invented intermediate totals. Task4 requires unfiltered just check, just browser and browser TypeScript check green. Do not silently use stale binary or install an unpinned replacement tool. Normal documented prerequisite installation follows environment permission policy.

Inspect each C1–C16 against your complete Rust delta and report checked/no finding or exact finding and action. Clippy/ast-grep are not substitutes for semantic conventions checks. Root verifies command results and records/routs handoffs only; root does not perform code, test-correctness, or semantic conformance review. Dedicated model review belongs to the designated review stage after the all-green barrier, not an extra root acceptance pass. This operator clarification supersedes contrary root-review instructions in the plan and older dispatches; it does not change executor implementation/check obligations. Return a handoff even if stopped: assigned task, files/diff, baseline, tests-before-body evidence, commands and full logs, actual totals, C-ID checklist, dependencies, genuine escalation sequences and done/blocked state.

Evidence files: docs/execution/per-84/run2-task<N>-<label>.* and run2-task<N>-handoff.md (N assigned). Use apply_patch for code/doc edits; tool command logs may be captured directly. No overwriting historical evidence. No review subagents, extra agents, independent review, or next-task dispatch by executor. At final full-execution completion only, the final executor appends the plan-mandated completion stage-event after confirmed all-green and resolved conventions; root records durable evidence and hands off to independent reviewer.

## Task1 starting state

The real 12 foundation/acceptance tests were written before bodies in checkpointB; root reproduced missing-approved-API compile-red at68. Read docs/execution/per-84/task-1-restart-b-handoff.md and task-1-restart-b-test.txt as the predecessor evidence. Interrupted implementation has begun approved domain/error additions; console.rs still holds the real tests. Inspect and complete this partial work; do not roll it back or claim a new red for already written portions. Preserve the test assertions; complete approved foundation APIs, proper docs and displays, run actual tests/gates and fix in scope. Do not add Reader or serve placeholders. This is a continuation from genuine pre-body tests, not a new planning stage.
