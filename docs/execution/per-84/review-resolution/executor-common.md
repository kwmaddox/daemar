# PER-84 review repair: shared executor contract

You are a fresh `gpt-5.6-luna` executor at `/Users/kendall/code/github/daemar`. Read your numbered packet, `docs/plans/per-84-review-resolution-plan.md`, the disposition ledger beside this file, root AGENTS.md, CONTEXT.md, conventions.md, `../contract.md`, and selected `governing-decisions.json`. Read `/Users/kendall/.agents/skills/tdd/SKILL.md` plus its tests.md and mocking.md references. Do not read memories, chat, unrelated Card history, reviewer definitions, old failed plans, or conduct a broad review. Do not spawn agents. Apply only your packet, including its narrow repair loop. All implementation is your responsibility; root does not supply semantic fixes.

The operator supplied findings as guidance. The dedicated planner has dispositioned them in `dispositions.md`; implement those bounded dispositions. No approval is needed for the already approved private test seams, behavior-preserving extractions, or specified repairs. Preserve public signatures, error variants, and external behavior except the prescribed standards-conforming Allow header. No mutation of acceptance harnesses/features/browser files, migrations, root Cargo.toml, gate/lint/policy files, instructions, unrelated docs, real factory DBs or unrelated dirty work. No commits, Card writes, review stage, hosting, or external messages.

Before edits, record HEAD, `git status --short`, diff and SHA256 of owned files, and current relevant tests. Store evidence only in your numbered `evidence/` directory (may create it). The current tree contains admitted untracked implementation and tests; preserve it and include relevant untracked source in snapshots/manifests. For protected baselines use `../admission.md` and `../run2-final-repair-hashes.txt`; do not rewrite them. At handoff verify no protected files changed during your packet. The admission plan hash itself is historical, not a requirement to restore an old plan. Do not read reviewer definitions merely because admission hashes name them.

Use apply_patch for source/docs edits. You may generate diagnostic/log/hash/snapshot artifacts with ordinary tools. Keep complete command output, cwd, exit status, and source snapshot/hash for each initial and final test run; never only a tail, grep result, or handwritten claim of green. Evidence filenames must distinguish retries. Record permission failures separately and follow the actual environment escalation tool. Do not let a pipeline's tee exit stand in for the command exit. Keep source provenance distinguishable from executor reports and root command verification.

Within each behavior slice: add/strengthen the named test, run it before behavior changes, record true initial pass or true failure, repair only actual behavior defects, rerun. For approved seam extraction, retain baseline/after verification and source evidence; no forced red. Prior missing historical red remains a limitation, not a blocker. Do not claim that moving code after green retrospectively creates test-first provenance.

Each packet runs `cargo test --locked -p daemar-card --lib`, its named focused checks, `cargo clippy --all-targets --all-features -- -D warnings`, `git diff --check`, and unfiltered `just check` from repository root. Formatting corrections are normal owned-file work. Finish ordinary failures in owned scope without per-fix confirmation. Never filter, ignore, weaken or mark expected failures. Packet03 additionally runs the final browser and TypeScript gates and may repair predecessor-owned files as explicitly transferred in its packet.

Write `evidence/NN/handoff.md`: implementation summary by CE ID; exact changed paths; tests/seams and actual initial/after results; commands with full log links and actual exits; C1–C16 checklist with sites/reasons (not just 'Clippy passed'); protected-file verification; source hashes; dependency changes; unresolved limitations. Retain existing review refutations and historical process deficiencies honestly. Report completion only after your packet's required commands complete successfully, or report a concrete missing-authority/environment blocker with all evidence. No hypothetical approval flow.
