# Packet01 — shared existence query and dependency cleanup

Execute as fresh gpt-5.6-luna. Read `executor-common.md` first. Predecessor is the existing candidate, recorded by `../run2-review-ready.md` and `../run2-final-repair-hashes.txt`. Output directory: `docs/execution/per-84/review-resolution/evidence/01/`.

Own only `crates/daemar-card/src/storage.rs`, `crates/daemar-card/Cargo.toml`, narrowly necessary `Cargo.lock` updates, and evidence/01. Read storage contract, current storage/domain/error interfaces, relevant existing storage tests and Store/Reader callers. No source edits outside these files.

1. Run baseline library tests and preserve full result. For CE-E, consolidate only the three `SELECT 1 FROM cards WHERE card_id = ?1` operations in Reader::entry, Store::require_card, and history_from_pool into a single private async helper. It takes borrowed pool/CardId and the caller's typed StorageContext and returns `Result<(), Error>`. Reader::entry retains ReadEntry, history retains ReadHistory, append retains AppendEntry. No public API, pool getter, transaction boundary, schema verification, query semantics or read-only capability change. Preserve CardNotFound versus EntryNotFound and typed SQL source. Store's existing private wrapper can delegate or its caller can call the pool helper directly; do not keep duplicated SQL.
2. Existing public Store/Reader tests are the regression seam. Run them before/after extraction. Verify existing absent Card, absent/foreign entry, and Store write/history cases still exercise the call paths; add a small missing case only if needed to pin a context or error distinction affected by this extraction. Do not test the private SQL helper by duplicating its query as an expected value. Do not manufacture a behavior failure for this behavior-preserving consolidation.
3. For CE-H, remove only the unused direct `hyper = "1"` entry. Preserve hyper-util and its feature list, serde_json arbitrary_precision and all unrelated manifest content. Cargo may need an unlocked offline `cargo check --offline -p daemar-card` to update the root package edge in Cargo.lock; that narrow mechanical lock update is authorized. Do not run a broad cargo update or remove transitive hyper. Inspect the lock delta and rerun `--locked` checks. If a resolved dependency genuinely needs the direct feature contribution, establish that from its manifest/compiler before changing the proposed removal; do not guess or weaken dependency policy.
4. Run common checks including complete just check, resolve ordinary owned failures, write handoff and final owned-source hashes. Completion requires the shared query has one owner, original caller contexts remain, no unused direct hyper declaration, all existing behavior acceptance passes, and no protected-file modifications. Record C10/query ownership and all other impacted conventions explicitly.

Next packet is02; do not start it or write its source.
