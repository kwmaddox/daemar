# Packet02 — HTTP conformance and meaningful failure/absence tests

Execute as fresh gpt-5.6-luna. Read `executor-common.md` first and `evidence/01/handoff.md`. Output directory: `docs/execution/per-84/review-resolution/evidence/02/`.

Own `crates/daemar-card/src/console.rs`, optional private test module files below `crates/daemar-card/src/console/`, and evidence/02. Existing `crates/daemar-card/templates/` files are additionally owned only for a minimal repair justified by an actual failure in the prescribed tests below; record that evidence before the template edit. Read console contract and R1/R6, existing console/router tests, domain payload constructors, Store fixture patterns, and the current templates. Protected `tests/behavior/console/{mod,dom,http}.rs`, console.feature, and browser/tests/console.spec.ts may be read as behavioral referents only. No dependency changes, template redesign, or extra presentation scope.

Use the existing private router and `rendered_result` seams, as previously approved. Storage fixtures are real temporary databases: create/append through Store, open Reader normally, then use a separate test-only SQLx connection to the same temporary DB to inject row corruption. Never give production handlers Store or a writable connection. No production pool accessor, permissions/chmod-based unreliable fixture, mocks of Reader, or real factory DB. A small test fixture may retain tempdir/path, Store and Reader; give it enough lifetime for the request. Name repeated chosen fixture values per C14, retain independent expected strings.

Perform slices in this order, saving each initial and after result:

1. CE-C: strengthen unsafe-method router tests to assert 405, Allow equal to `GET, HEAD`, and empty body for root, a Card path, unknown path and CSS. Exercise at least existing POST/PUT cases; keep foreign-Host plus unsafe-method precedence at empty421. Observe the missing-header failure first, then add the header to the existing method guard. Do not alter route matching, permit writes, introduce OPTIONS, or move the host guard inward. RFC citation is corrected to9110 §15.5.6 in the ledger.
2. CE-I HTTP: replace `host_rejection_precedes_unreadable_storage` with a real failure fixture. After opening Reader on a valid DB with a Card, corrupt a column queue must decode (e.g. cards.created_at) using the separate connection. On the same router state, an accepted-Host GET `/` must yield storage500, proving the fixture reaches the failure path; foreign-Host GET must yield421 with empty body. This pins the response precedence claim; do not claim a query-count instrument. Reuse the corrupt-queue setup for `queue_failure_has_no_queue_or_card`: assert500, HTML storage-failure indication, and absence of queue/queue-row, Card identity, stream, inspector and payload markers. For `stream_failure_has_queue_without_partial_card`, create a real target Card with at least one appended entry, corrupt only its payload JSON (queue must remain readable), verify queue success as a control, request that Card and assert500, visible retained queue/title, error marker, and no partial Card identity/stream/inspector/payload. Do not rename success tests and count that as repairing the named cases.
3. CE-K: make `render_failure_returns_fixed_plain_text_500` asynchronous if needed for body collection. Call the existing rendering-result seam with the sentinel Askama failure and assert500, exact content type `text/plain; charset=utf-8`, and exact body `console rendering failed`. The sentinel must not leak. No production render failure trigger or new public error.
4. CE-L HEAD: compare GET/HEAD on the same immutable fixture for `/`, an existing Card with selected inspector, CSS, and missing route/Card. Assert expected status, matching content type where present, and empty HEAD body; GET controls prove nonempty bodies on content routes. Do not compare transient Date headers or require a nonexistent header. Rely on the actual Router's HEAD handling. If these already pass, record initial pass without rewriting working routing.
5. CE-L payload absence: append a StageEvent with stage+summary and no optional payload using existing typed constructors, select its actual entry ID through a Card URL, and assert200 with inspector/stage/summary present but **no payload element**. Add/reuse a payload-present selected entry as a positive control asserting a payload element and its independent expected text. Do not equate omitted payload with JSON null or assert absence only on error pages. Assertions may use existing dev-dependency DOM facilities or precise element markers with positive controls; no new dependency/harness mutation.
6. CE-F: at the actual Arc::new(reader) shared-state introduction, state that Axum requires Clone state shared across concurrent request tasks and Reader is read-only so sharing carries no mutation. This is a documented C12 reason, not a code architecture change.
7. CE-G: replace only `schema_version: 1` in entry_view with `entry.payload.schema_version()`. The ledger explains why the former refutation is rejected. Use existing schema-version inspector acceptance as regression evidence; no synthetic new schema version or public API change. Preserve StartupWire, all public Startup accessors and their wire-equivalence tests (CE-D remains refuted).

Run each named focused case with `cargo test --locked -p daemar-card --lib <case-name>`; ensure it runs at least one matching test. Then common full library/Clippy/just check checks. Complete the ordinary implementation/test-fixture repair loop before handoff. The baseline currently satisfies many strengthened behaviors, so initial green is expected and valid evidence. Handoff identifies each repaired CE and tests, any behavior changes beyond the Allow header (must be an actual failure within accepted scope), fixture controls, final source hashes and C1–C16 impacts. No root semantic review is requested.
