# Packet03 — CLI sequencing/output and complete final gates

Execute as fresh gpt-5.6-luna. Read `executor-common.md` first and both predecessor `evidence/01/handoff.md` and `evidence/02/handoff.md`. Output directory: `docs/execution/per-84/review-resolution/evidence/03/`.

Initial owned source is `crates/daemar-card/src/main.rs` and optional private binary test modules below `crates/daemar-card/src/`. Read approved main.rs flow, public console/Reader signatures, current binary tests and selected governing decisions. The private CLI orchestration and output-control seams are already approved by Task4. Preserve CLI wire formats, config precedence, Reader-only console open, one bind with no retry, and the exact library API.

1. CE-A: import/use `console::DEFAULT_PORT`; remove `DEFAULT_PORT_TEXT = "7331"`. Derive clap's default string once from `DEFAULT_PORT.get()`. The locked clap lacks its string feature, so provide a private static `std::sync::OnceLock<String>` whose initialized string is borrowed as `&'static str` by `default_value`; explain the lifetime/cache purpose. No dependency feature addition, Box::leak, second chosen value, or arithmetic hardcoding. Add a parser default test checking independently expected7331, preserve explicit0 and invalid/out-of-range rejection. Existing library default tests remain independent.
2. Extract the existing production serve flow into a small private orchestration function with injectable **publication operation** and **server continuation**, both consumed once. Keep configuration/Reader open/bind/startup construction in the real flow. Publication takes `&Startup` and returns `Result<(), daemar_card::Error>`; continuation takes ownership of the actual `Listener` and `Reader` and returns a future with `Result<(), daemar_card::Error>`. Production delegates publication to `publish_startup` using a scoped stdout lock, and continuation to `console::serve`. Drop the stdout lock when publication finishes, before awaiting the indefinite server. Do not build a generic command framework or mock Reader/bind. Keep helpers private; no new library public surface. Preserve before/after evidence for extraction and wire it into the actual `serve` command path before claiming its tests cover production.
3. CE-J: replace `publish_failure_is_domain_and_prevents_serve` with tests running that orchestration on a real temp migrated DB and port0. The injected publication operation calls real `publish_startup` against failing writers: cover both write and flush failure (the latter may leave one line). A local observation at the continuation must prove it was never invoked, not just that an Error value converts. Assert Domain(PublishStartup), source kind/sentinel and existing error category. No process-global stdout replacement, timed sleep, or fake assert-false. Test write and flush cases separately or as independently asserted fixture cases.
4. CE-I CLI: replace `serve_uses_published_listener` with the real orchestration. Capture the published Startup using the real publisher/recording writer; at the continuation verify publication/flush already completed and the received Listener.bound() matches that startup bound/URL, with nonzero assigned port. The helper must move the original binding into that continuation; never rebind for the test. Return a completed success from the injected continuation so the test terminates deterministically. Observing equal startup-derived values without observing the received Listener is insufficient.
5. CE-B: extract the current success-output branch into a private writer boundary used by main for actual `Control` results; write the existing JSON line for Print and no bytes for Served. Keep error rendering/exit behavior on the existing paths; do not expose a public printer. Replace `serve_emits_no_second_success_line` with a test that runs the production orchestration to completion via the injected continuation, sends its actual returned Control through that **same production success-output boundary**, and inspects all recorded stdout bytes. Assert exactly one newline-delimited startup object with expected DB/source and the published bound URL, and zero appended success bytes. Include a Print control positive test showing ordinary commands still emit their JSON plus newline. A single `Control::Served` equality or a publisher-only test does not cover this requirement. Use local recording writer/RefCell where necessary for observation; no extra process/thread framework.
6. Existing reader/bind/serve mapping tests may remain; do not broaden this assignment into replacing every test. The new orchestration must retain their behavior and all accepted external scenarios. Record actual initial passes where behavior already works, and honest extraction provenance. Missing historical red remains a final limitation; do not backfill it.

Run focused binary cases with `cargo test --locked -p daemar-card --bin card <case-name>`, checking nonzero matching counts, then all binary tests `cargo test --locked -p daemar-card --bin card`, and common checks. Save complete outputs/exits.

For the final check/fix loop only, this packet explicitly transfers predecessor ownership of `crates/daemar-card/src/storage.rs`, `crates/daemar-card/src/console.rs` and its private tests, crate `Cargo.toml`, and narrow `Cargo.lock` edits to you. You may fix ordinary failures directly caused by the numbered repairs or uncovered by their new tests within already accepted behavior. Do not change completed dispositions, reopen CE-D, or perform a broad cleanup. Existing templates may be changed only if an actual final failure within these prescribed behaviors demonstrates the minimal need; this final packet grants that narrow ownership. Protected external tests, policy and public surface remain fenced. Include a separate evidence explanation for any predecessor/template repair.

After all code/fixtures are final, complete these unfiltered commands on the same candidate:

- `just check`, cwd repository root; every gate stage must run successfully.
- `just browser`, cwd repository root; it builds current card before all browser tests. Verify the existing local Playwright executable/package first so npx cannot silently substitute a package. Use the repository browser-install workflow only if actual missing prerequisites require it, with normal permissions and without changing package files.
- `./node_modules/.bin/tsc --noEmit`, cwd `/Users/kendall/code/github/daemar/browser`.
- `git diff --check`, plus protected-file/hash verification against predecessor and admission evidence.

Own the ordinary check/fix loop through actual completion. Preserve each full failed output and subsequent successful output with actual exits; do not hand back a failed advisory check or first compile error as a completed task. Follow environment escalation on permission failures. If code changes after the final runs, rerun affected focused tests and all three final gates. No skipped accepted tests, stale binary evidence, filtered final command, dependency-policy edits, retries/timeouts added to acceptance, or softened assertions.

Final `evidence/03/handoff.md` must cover CE-A..CE-L using the ledger, exact source/candidate hashes (including changed untracked files), complete command/log/exit table and actual measured counts, protected-file verification, executor C1–C16 resolutions, task/model provenance, dependency delta, CE-G promotion rationale, CE-D retained refutation and CE-I historical limitation. Explicitly distinguish executor claims from root's independent command observations. No Card writes or commits; return paths and completion evidence to root for recording.
