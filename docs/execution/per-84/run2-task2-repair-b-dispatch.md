# PER-84 Task 2 evidence repair B

Fresh Luna executor, same accepted Task 2, not a new plan. Read active-dispatch.md, contract.md, current plan Task 2, AGENTS.md and conventions.md. Current predecessor is run2-task2-repair-handoff.md, which root has NOT accepted. Keep its valid source/tests/results; repair only storage.rs/lib.rs and private storage tests plus run2-task2-repair-b-* evidence.

Root diagnosed the repeated incomplete handoff before this narrowed dispatch. Resolve these specific gaps:
1. reader_connection_is_read_only_and_sees_live_writer_append uses INSERT missing cards.created_at. Root ran that exact INSERT against an ordinary writable in-memory database using the unchanged migration: exit19 NOT NULL constraint failed: cards.created_at. Therefore is_err proves nothing about readonly. Use a valid write with a writable control and assert the actual SQLite readonly error from Reader; verify unchanged stored value. Preserve live visibility test.
2. reader_entry_membership_distinguishes_card_and_entry_absence never creates a second real Card. Create two actual Cards, select one Card's actual entry under the other existing Card and assert EntryNotFound, while preserving own-entry success and missing-Card distinction.
3. schema_comparison_precedence_covers_all_shapes tests a copied .find/.map expression for Dirty, not production classification; empty currently tests the partial helper's Behind, not required full Uninitialized precedence. Put full deterministic classification behind one private seam called by production and tests; test dirty vs simultaneous version/checksum mismatches, empty/uninitialized, exact, nonempty Behind, Ahead, version divergence, checksum, earliest competing mismatch. No new migrations or public API.
4. Refusal-without-mutation is claimed but only empty file length is checked. Add actual main bytes, permission metadata and bookkeeping comparisons before/after Reader open/refusal on empty and dirty/checksum fixtures. Ensure the writer is quiescent so its own checkpointing is not confused with Reader mutations. SQLite coordination files remain allowed; use temp fixtures only.
5. queue latest-sequence test has increasing timestamps and cannot distinguish MAX(timestamp) from latest sequence. Use out-of-order timestamps with the later sequence's time earlier; assert later sequence still wins. Selected-column corruption test covers only entry timestamp; add actual Card created_at corruption rejection. Preserve creation ordering and payload-corruption success tests.
6. C10: recurring unnamed schema tuple shapes in production signatures/test helper are not addressed by 'no duplicate domain type'. Use a named private representation and document any divergent related representations. C14 checklist says no repeated fixtures but new tests repeatedly embed chosen fixture values. Inspect full Task2 delta against exact C1-C16, resolve concrete violations, and cite accurate checked sites. C15 is about semantic bool parameters, not query ownership. Do not invent additional policy.
7. Predecessor has no saved initial-test run/source snapshot; don't assert retrospective TDD compliance. Capture these corrections' test source and real initial results before corresponding production fixes; existing passing behavior stays passing. Explicitly preserve the historical evidence gap in the new handoff.

All ordinary implementation repairs are yours. No chat hints, old plans, reviewer agents/definitions, independent review, other agents, root source edits, external tests, policy changes, later-task work or commits. All public API and accepted product behavior stay fixed.

Run cargo fmt --all, cargo test --locked -p daemar-card --lib, strict Clippy and unfiltered just check, escalate loopback/advisory permission when needed and poll to actual exit. Save COMPLETE actual output/cwd/exit using execution command logs; no summarizing command output into a purported full log. Only named missing-serve successor failures may remain. Report all concrete obligations resolved with test sites and exact evidence, or a genuine contract/authority conflict through the Card protocol. Output docs/execution/per-84/run2-task2-repair-b-handoff.md. Root accepts then dispatches Task3.

