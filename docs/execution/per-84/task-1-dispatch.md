# PER-84 Task 1 dispatch — Luna foundation executor

You are the fresh Task 1 executor, model gpt-5.6-luna. Execute only Task 1 of docs/plans/per-84-luna-execution-plan.md, approved at Card 56 and cleared at 58; operator has authorized execution. Read the entire plan's common fences/protocols and Task 1, docs/execution/per-84/contract.md, docs/execution/per-84/admission.md, current AGENTS.md, conventions.md, CONTEXT.md, and Task 1's named repository sources. These are your factory inputs; no prior conversations, session summaries, discarded plans, unrelated Card entries or historical review discussions. External ticket is unavailable; accepted executable specs plus supplied approved contract are the referents.

Work in /Users/kendall/code/github/daemar. All code and code fixes are yours, not the orchestrator's. No other implementation writer is active. Only Task 1 production files are authorized: crates/daemar-card/src/{domain,error,lib,console}.rs, crate Cargo.toml and necessary Cargo.lock changes, crate-local unit tests within owned modules. Do not implement Reader, HTTP serving, templates, or CLI integration yet. All accepted public surface is exactly the supplied skeleton; do not invent public test seams. Preserve the dirty admission state and protected files. Do not change deny.toml or another gate/policy file without a recorded ruling.

The TDD skill applies: read /Users/kendall/.agents/skills/tdd/SKILL.md and its required references. The approved plan is the user-confirmed seam/test selection and overrides generic skill guidance where it explicitly requires private seams. Mandatory subtle-test ordering and append-and-stop apply exactly as written; do not manufacture evidence, silently waive behavioral red, or inject artificial test failures. Record actual red/green commands, exit codes and outputs. Do not request conversation hints to fill contract gaps: append the question to the Card and stop. No production limitation comments in lieu of execution.

Authoritative Card database: PER84_CARD_DB=/Users/kendall/.daemar/daemar.db. Use the existing absolute CLI /Users/kendall/code/github/daemar/target/debug/card for mandated append commands (avoid another program named card and avoid rebuilding a broken workspace to record escalation). Card ID 01a0693e-bc16-7272-9ce9-a20f3b07f875. Only the plan's escalation decision is applicable before full implementation completion; no review records. If sandbox permission is needed use require_escalated with scoped justification. Dependencies/network and loopback/advisory-db diagnostics use normal permission escalation; failures are never waived.

Evidence output ownership: docs/execution/per-84/task-1-handoff.md plus new task-1-* evidence files in docs/execution/per-84/. Use apply_patch for document/code edits; tool output may be captured as execution logs. Include owned-file baseline and final changes, exact dependency versions/deltas, named test red/green evidence, full gate outcomes and deferred successor-scope failures, C1–C16 checklist, any escalation sequence and clear complete/blocked verdict. Run the Task 1 acceptance commands. Existing S3 missing-serve failures can remain only as the approved intermediate state. Do not commit, run independent reviewers, spawn additional agents, or proceed to Task 2. Return your handoff path and status. Any gap requires durable Card escalation then stop, not side-channel fixes.

