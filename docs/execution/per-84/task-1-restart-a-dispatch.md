# PER-84 Task1 restart checkpoint A — genuine first test

Fresh Luna executor; Card recovery decision63 restarts the rejected Task1 from the admitted baseline. Implement ONLY this bounded first-test checkpoint, not all Task1. Current workspace /Users/kendall/code/github/daemar. Factory inputs: current AGENTS.md and conventions.md, CONTEXT.md, docs/execution/per-84/contract.md, docs/plans/per-84-luna-execution-plan.md common rules and Task1, docs/execution/per-84/task-1-restore-handoff.md. No prior transcripts, failed implementation archive, old plans, reviewer discussions or memories. Retain BSD-3-Clause authorization61; no license change required.

Read the named Task1 interfaces, then write a meaningful crate-local listener_zero_reports_bound_port test through the approved public Listener/LoopbackAddr/Port boundary. It must observe the real bound listener's nonzero port after requesting Port::EPHEMERAL, not assert a constant failure. Read /Users/kendall/.agents/skills/tdd/SKILL.md and its references; approved plan supplies confirmed seams. Do not write any Listener/startup implementation body yet. You may add only the module/test wiring and minimal needed Tokio net feature to compile the test if otherwise available; do not install Axum/Askama yet, invent APIs, add placeholder production failure behavior, or implement code just to manufacture a red. If missing approved API yields compile failure, capture that precisely, don't mislabel it behavioral red. The plan explicitly requires behavioral red before bodies; if it cannot be established without breaking that fence, follow its append-and-stop protocol with the precise question and evidence. Do not guess an exception.

Write scope: crates/daemar-card/src/console.rs for this test; src/lib.rs minimal module wiring only; crate Cargo.toml and Cargo.lock only if needed for Tokio net; docs/execution/per-84/task-1-restart-a-* evidence files and task-1-restart-a-handoff.md. No other code, tests, policy or documentation edits. Run the exact focused test; save complete stdout/stderr, command, exit status plus hash of the tested files. Report and STOP at this checkpoint before any implementation, even if a genuine behavioral red is obtained. Orchestrator will validate admission evidence before fresh implementation dispatch; no chat hints or continuing based on a private message.

Card protocol: DB /Users/kendall/.daemar/daemar.db; Card01a0693e-bc16-7272-9ce9-a20f3b07f875; CLI /Users/kendall/code/github/daemar/target/debug/card. For record gaps append a decision using ONLY summary/reason JSON fields, producer implementer, kind agent, explicit --db. Use environment require_escalated for Card writes or restricted socket/advisory operations; do not mask sandbox failures or claim append success without returned sequence. No implementation completion event because this is only checkpoint A. No reviews or other agents. Root handles evidence recording and checkpoint acceptance concurrently.

