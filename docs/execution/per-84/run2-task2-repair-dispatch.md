# PER-84 Task2 incomplete-handoff repair

Fresh Luna executor; continue the SAME Task2 under docs/execution/per-84/active-dispatch.md, approved plan Task2 and contract.md. Root rejected predecessor completion, not product scope: just check exits1 at rustfmt; only a happy-path test exists; required schema/read-only/corruption/membership cases, full gate rerun with necessary permission, C1–C16 check and saved command evidence are missing. Do not repeat the predecessor's complete claim. This is ordinary execution repair, not a re-plan or authority to touch accepted external tests.

Write scope: storage.rs/lib.rs and crate-local/private storage tests, plus run2-task2-repair-* evidence/handoff. Preserve Task1 and all protected files. Current Task2 code is unaccepted work available to repair; archive /private/tmp/per84-execution-GGJtBp/task2-unaccepted.tar.gz preserves it. Do not claim retrospective red evidence for bodies already present. Write the missing cases now BEFORE any corresponding repair; save actual initial passes/failures with source snapshots. Existing initially-passing behavior stays passing; don't break correct code or invent assertions to manufacture red. For fixes driven by real failing tests, save red then green. Existing accepted external feature tests also predate this implementation; do not conflate their absent-serve red with proof of private branch coverage. Report the predecessor evidence gap honestly.

REQUIRED Task2 acceptance cases (implement all, separately recognizable names or clear table cases):
- pure schema classification cases exact/empty/uninitialized, dirty wins over competing mismatch, nonempty proper prefix Behind, first surplus Ahead, same-position version Diverged, checksum mismatch, first mismatch when several differ. Use synthetic multi-migration test data; never add a migration. Private comparison seam may be refactored within source ownership to cover complete precedence.
- actual open: missing file not created; existing empty/uninitialized refusal; current migrated file success; mismatched/dirty bookkeeping refusal unchanged. Snapshot main bytes/permissions/bookkeeping where promised; SQLite coordination files allowed.
- actual Reader connection refuses writes; normal reads work; live Reader sees append from separate writer without reopening. Reader open must remain normal read-only WAL, no journal_mode call, immutablefalse, no create/migrate/chmod/writer fallback, exactly-one configured pool connection.
- queue creation order and greatest-sequence timestamp, ignores corrupt entry payload, rejects corrupt selected timestamp/card columns.
- entry existing membership success; unknown Card vs absent entry distinction; foreign Card entry rejected.
- Store list/history share Reader queries and existing Store write/read regression tests remain green; all Task1 tests preserved.

Do not stop after writing just one smoke test. Test source may use private cfg(test) helpers and temporary DBs; no real Card DB as fixture. Use existing typed payload/IDs and convention-compliant named schema shapes rather than repeated unnamed tuples or strings where typed values exist. Keep verifying-schema query I/O separate from pure classification; use helpers for clear concerns, not invented framework. All pub signatures exactly approved, with full invariants docs.

Execute cargo fmt --all; cargo test --locked -p daemar-card --lib; strict Clippy; just check. For loopback/advisory-db restrictions, rerun via require_escalated with scoped justification: documented permission failures are not permission to stop verification or report completion. Save COMPLETE stdout/stderr/cwd/exit for every claimed command using evidence files. If command returns running session, poll until actual exit. Final just check may still fail ONLY external absent-serve S3 cases57pass62fail, not formatting or unit tests. Record all C1–C16 checked with sites/reasons or repaired findings; Clippy is not the semantic checklist.

Read no prior chat, memories, other Cards or old plans. Ordinary in-scope compile/test/lint repairs proceed without asking root for hints. Genuine unresolved contract/policy conflicts use executor Card decision append-and-stop (explicit DB /Users/kendall/.daemar/daemar.db; Card01a0693e-bc16-7272-9ce9-a20f3b07f875; absolute existing target/debug/card; producer implementer/kindagent; JSON summary/reason only; permission escalation). No reviewer/other agents, commits, Task3, or completion stage-event. Root independently checks acceptance after handoff; only you write source. Output docs/execution/per-84/run2-task2-repair-handoff.md.

