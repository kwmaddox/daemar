# PER-84 Task1 restart checkpoint C — implement tested foundation

Fresh Luna implementation executor. Root accepted checkpointB's meaningful pre-body tests under ruling66 and reproduced missing-approved-API compile-red (exit101). Test source snapshot was archived before bodies at /private/tmp/per84-execution-GGJtBp/task1-restart-b-tests.tar.gz SHA256560cbc2e5a8dcb5e149e53deb5b6727e08b7e87c09429c5b094107fb3df771f1; source content is also recorded on Card. Implement Task1 only from these tests/approved contracts; do not reuse failed attempt code or transcripts.

Inputs: current AGENTS.md, conventions.md, CONTEXT.md, docs/execution/per-84/contract.md; current docs/plans/per-84-luna-execution-plan.md Task1 and all shared/final/repair rules; docs/execution/per-84/task-1-restart-b-handoff.md, test.txt, hashes.txt; existing Task1 repository sources and acceptance fixtures. Plan current SHA256 e96412d462ef17d4f2c610354edb83dce6498e6800a69450e2c4e1b3e58deea6. No old plans, failed attempt archive/code, conversations, unrelated Card history, reviews or memories. All brief-listed paths live under /Users/kendall/code/github/daemar unless absolute.

Ruling66: missing-approved-API compile-red counts as valid initial red; record it honestly. Artificial assertion failures, deliberately broken stubs and environment failures do not. Already satisfied later tests are honest initial passes tied to the prior slice's red/green evidence, not fabricated reds. Preserve tests-before-body. Read the TDD skill and references; user-confirmed seams are in the approved plan. Existing Task1 pre-body tests now cover the required behaviors; use them, run each meaningful green outcome after its corresponding body. If new behavior needs a new test, write it first.

Allowed implementation files: crates/daemar-card/src/{domain,error,lib,console}.rs, crate Cargo.toml and necessary Cargo.lock. Implement approved Task1 domain/error surface plus Port/LoopbackAddr/Listener/Startup/ConfigSource/publish_startup exactly. No Reader implementation/re-export before Task2; no serve body or templates/CLI before later tasks. Maintain existing error/source/CLI behavior. Dependency allowance for BSD-3-Clause already landed in deny.toml under decision61; preserve it and every other protected file. Dependency additions limited to Task1 plan-authorized Axum/Askama/Tokio minimal features; no unrelated resolution churn or policy changes. Do not widen public signatures, add placeholder unsupported branches or public test seams.

CheckpointB test assertions are accepted for implementation admission. Preserve their semantic expectations; mechanical formatting/import/lint corrections are allowed and must be listed. If an assertion is genuinely inconsistent with the approved contract, do not rewrite it to fit code: use Card escalation with exact evidence. Unit-test cleanup must not weaken assertions. The existing test source snapshot remains immutable evidence even if a mechanical correction is necessary in working code.

Verification: capture actual per-test/full unit green outputs and use normal require_escalated for loopback/advisory-db restrictions. Run cargo test --locked -p daemar-card --lib; cargo clippy --all-targets --all-features -- -D warnings; just check. Intermediate S3 missing-serve failures are allowed only as described in Task1; all S1/S2 and all unit tests must pass. Save complete command outputs, cwd/exit, exact source hashes and tests initially passing vs compile-red history in new docs/execution/per-84/task-1-restart-c-* artifacts. Write task-1-restart-c-handoff.md. C1–C16 each need an individual checked/no-finding or finding with site/reason, not 'Clippy passed'. Root runs independent deterministic and conventions gates afterward; code is not accepted merely because you report green. Fix your in-scope implementation/gate issues; if a rule cannot be met without a scope change escalate, don't waive.

Only one implementation writer. No commits, review agents, other subagents or Task2. Orchestrator never writes code. Card01a0693e-bc16-7272-9ce9-a20f3b07f875, authoritative DB /Users/kendall/.daemar/daemar.db, CLI /Users/kendall/code/github/daemar/target/debug/card; record-gap escalation as decision with ONLY summary/reason, producer implementer/kind agent, explicit --db, normal permission escalation. Stop after Task1 handoff; no full-implementation completion event yet. Return done/blocked and artifact path.

