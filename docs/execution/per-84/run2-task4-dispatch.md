# Task 4: CLI integration and final execution gates

Activated after root checked the Task 3 continuation command logs: formatting and strict Clippy exit 0; library 60 passed, zero failed; just check exit 101 with 57/119 scenarios passing and 62 missing-serve successor failures. This is command-result verification, not a semantic review verdict.

Fresh Luna executor. Read AGENTS.md, CONTEXT.md, conventions.md, active-dispatch.md, contract.md, current plan Task 4 and shared fences/final barrier, and run2-task3-continuation-handoff.md with linked evidence. All execution paths here are under docs/execution/per-84 unless specified. Read the task's named source and accepted executable tests. No chat, memory, old plans or reviewer definitions.

Execute the complete approved Task 4 CLI implementation, named tests and check/fix loop. Own crates/daemar-card/src/main.rs and private binary tests plus run2-task4-* evidence. Preserve predecessor code, accepted external tests, approved API and policy. Any predecessor-source repair needs a scoped ownership transfer from root, not a self-granted expansion. Root provides no semantic diagnosis or review. Do not spawn agents or commit.

Capture genuine initial tests/results before corresponding bodies. Missing approved API compile red is valid. Initial passes stay passing and historical gaps remain honest; no artificial failures. Use the plan's already-approved private seams. Continue ordinary implementation and deterministic/conventions repairs through completion, not merely first compilation.

Run unfiltered just check, just browser, and browser local TypeScript check exactly as the plan requires. All final gates must pass; missing-serve is no longer an allowed deferred failure. Use normal required permission escalation and documented browser prerequisites, never weaken/skip accepted tests. Save complete command output, cwd, exits, source/evidence digests and C1-C16 executor checklist. Poll running commands to actual exit. The accepted suite comprises 119 Cucumber scenarios and 11 browser tests; added cases may increase totals.

Root verifies command results and routes handoffs only. Dedicated semantic model review follows the all-green execution barrier. On genuine protected-file, API, dependency-policy or other missing-authority conflict, follow the active Card append-and-stop protocol. Do not manufacture a default or alter settled scope.

Once final gate conditions and the plan's completion obligations are met, append the executor-authored completion stage-event as required by the plan, using the explicit authoritative DB and Card in active-dispatch.md. Capture the returned sequence and entry ID. Return run2-task4-handoff.md with actual results and remaining limitations, if any. A failed gate or failed required append is not completion.
