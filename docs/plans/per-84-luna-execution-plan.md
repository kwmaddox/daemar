# PER-84 execution plan: fresh Luna task packets

## Authority and input manifest

This is a new plan for Card `01a0693e-bc16-7272-9ce9-a20f3b07f875`, authored from `/private/tmp/per84-factory-planning-input.md`: approved sequence 38 contract, binding sequence 45 rulings, and sequence 46 `ConfigSource::Display` amendment. The previous execution plan is discarded. Expensive models own planning, judgment, dispatch, and acceptance; fresh-context Luna-level agents perform **all implementation and fixes**. The orchestrator writes no implementation code. Independent review is a subsequent stage, not part of this plan or its execution conformance loop.

Read inputs were root `AGENTS.md`, `conventions.md`; `Cargo.toml`, `Cargo.lock`, `crates/daemar-card/Cargo.toml`; the five existing `crates/daemar-card/src/*.rs` files; `migrations/0001_cards.sql` inventory; `tests/features/console.feature`, relevant `tests/behavior.rs` and `tests/behavior/console/{mod,dom,http}.rs` contracts; `browser/tests/{console.spec,console}.ts`, browser package/configuration files; `justfile`, `scripts/check.sh`, `scripts/hooks/pre-commit`, `.gitignore`, `deny.toml`, `clippy.toml`, `sgconfig.yml`. Relevant locally installed source checked: SQLx SQLite 0.9.0 connection defaults and migration bookkeeping; Axum 0.8.9 serving contract. Task paths below are relative to the repository unless stated otherwise.

In task packets below, `src/`, `tests/`, `templates/`, `static/`, and `migrations/` are shorthand beneath `crates/daemar-card/`; `browser/` and root configuration paths remain repository-relative. Commands run at repository root unless an explicit `cd` is given.

The external ticket, full Card history, old reviews/plans, transcripts, memories, and unrelated research were not inputs. No additional product/design document is assumed. The feature and browser acceptance tests are the accepted executable scope. Before dispatch, the orchestrator must provide each executor the exact supplied contract text in a durable input artifact, with digest and provenance; a temporary pathname alone is insufficient for a later session. Attach this plan, admitted working-tree manifest, and the predecessor handoff. Do not ask the next executor to recover intent from chat or hunt old plans.

## Measured baseline and limits

Planning baseline: HEAD `4c809b31264f6c147563c52604d6aea598487ccd`, 2026-09-08. Existing user work is modified `.gitignore`, `Cargo.lock`, `crates/daemar-card/Cargo.toml`, `crates/daemar-card/tests/behavior.rs`, `justfile`; untracked `browser/`, console feature/harness, and unrelated `docs/research/per-83-story-review-refutation-ledger.md`. Preserve all of it. HEAD alone does not identify the accepted tests: capture hashes and contents of tracked modifications and relevant untracked files before implementation.

- `cargo test --locked --offline -p daemar-card --lib`: 31 passed.
- `just check` first stopped at cargo-deny's advisory database lock on a read-only path. The permission-escalated rerun reached completion: ast-grep's five rule-test groups, scans, format check, Clippy, cargo-deny, and 31 library tests passed; behavior finished with **119 scenarios: 57 passed, 62 failed**, exit 101. `serve` is absent from production; this is an implementation baseline, not permission to waive final failures. The stopped gate did not establish downstream doctest success.
- Node `v24.14.1`, npm `11.11.0`, installed Playwright `1.62.1`; `browser/node_modules/.bin/playwright test --list -c browser/playwright.config.ts` discovers 11 tests. Browser execution and Chromium launch were **not** verified. No dependency was changed or installed by planning.
- Sequence 45 R1 is already present: StageSummary is separate from DecisionSummary, Stage/StageSummary lack payload sinks, and `inspector_payload_equal` uses member presence. Preserve this; do not dispatch a redundant test rewrite.

At final planning inspection, concurrent work also modified `AGENTS.md` and introduced untracked `agents/` and `.pi/`. These are not planner changes; preserve them and refresh the admitted instruction/input manifest before dispatch. The added review workflow governs the subsequent independent-review stage, which this assignment does not launch.

No production console, Reader, or approved new error/domain API exists yet. There is one embedded migration. Consequently the feature's behind fixture currently produces zero applied migrations, classified **Uninitialized** by the approved precedence; it cannot prove the positive `Behind` branch by itself. Additional private classifier tests below address this without adding production migrations.

## Shared fences and working rules

All executors read `AGENTS.md`, all C1–C16 in `conventions.md`, the supplied approved contract, this plan, and their task's exact inputs. Work serially, one writer at a time; the foundation, Reader, console, and CLI share compilation and API dependencies. Finish and record one handoff before transferring ownership. Parallel implementation is unnecessary for this slice. Expensive agents may inspect and diagnose concurrently, but never patch code.

Allowed implementation surface, only as assigned below: `crates/daemar-card/src/{domain,error,lib,storage,console,main}.rs`, private submodules under `src/console/` if needed; HTML templates under `crates/daemar-card/templates/`; one stylesheet `crates/daemar-card/static/console.css`; crate manifest and narrowly required `Cargo.lock` changes. Add focused unit tests inside owned modules or private test modules. Preserve existing Store public signatures and all existing CLI wire behavior. Only the supplied approved public additions, including `ConfigSource::Display`, are authorized. Do not expose private HTTP/view/schema helpers or add new error variants to make implementation easier.

Protected: all existing feature files, Rust behavior assertions/fixtures, browser acceptance assertions/helpers/configuration, migrations, root gate/lint/dependency-policy files, `.gitignore`, `justfile`, unrelated docs, real factory databases, and all other files. Existing dirty manifest/lock changes remain intact. An executor may propose a demonstrable test defect but must return a reproducer and exact proposed diff for judgment; it may not weaken, skip, delete, mark expected-failure, add retries, broaden timeouts, or filter tests to obtain acceptance. Added focused tests must augment the existing oracle, not replace it. No commits, deployments, or unrelated refactors occur in these tasks. The only executor Card mutations allowed are the escalation decision and completion stage-event required below by sequence 42 and reaffirmed by sequence 55. This exception permits those appends to the supplied authoritative database only; review findings, refutations, verdicts, and adjudications remain exclusively the review orchestrator's records.

Per task: first capture the owned-file diff and relevant baseline. On every marked subtle step in Tasks 1, 3, and 4, and on Task 2's test-first work, write the named crate-local unit tests first, run them, and only then write the corresponding implementation body. This ordering is mandatory. Preserve the exact test names, command, exit status, and observed output in the handoff, followed by the green rerun evidence. A meaningful test failing compilation specifically because its approved API does not yet exist is valid initial TDD red; label it as missing-approved-API compile red, not behavioral red. It permits implementing the corresponding approved API and tested behavior without first manufacturing a behavioral failure. Once the APIs compile, tests for new, unmet behavior must fail on that actual unmet behavior before its corresponding body is implemented; artificial assertions or deliberately broken production stubs are not valid red evidence. If a later named case is already satisfied by a previous legitimate implementation slice, record its observed initial pass and that earlier slice's actual red/green evidence; the initial pass is not a new red event and does not require breaking correct behavior. These evidence rules apply to every task's red/green instructions below. Do not manufacture historical red, weaken tests, or implement untested code first. If required evidence cannot be established under these rules, use the append-and-stop protocol below. Then implement only that packet. Run focused tests and deterministic checks, inspect the entire changed Rust diff against C1–C16, fix findings, and rerun affected checks. Compilation and the task's focused acceptance must be green at handoff. Full behavior failures solely in explicitly unimplemented successor scope can remain recorded between tasks, but never at the final barrier.

**Executor escalation — append and stop:** when the supplied record cannot answer a question, the executor itself appends a decision to Card `01a0693e-bc16-7272-9ce9-a20f3b07f875` with producer `implementer`, kind `agent`, then stops. The dispatch packet supplies `PER84_CARD_DB` as the verified authoritative database path. Replace the JSON string values below with the exact question and supporting evidence/record gap, keeping the `summary` and `reason` fields and valid JSON:

```sh
card --db "${PER84_CARD_DB:?dispatch must supply the authoritative Card database}" append 01a0693e-bc16-7272-9ce9-a20f3b07f875 --entry-type decision --producer implementer --producer-kind agent --payload '{"summary":"PER-84 execution question: state the unanswered question","reason":"State the task, evidence, and gap in the supplied record; implementation is stopped pending a Card answer."}'
```

Capture the returned entry ID and sequence in the handoff. If the append fails, preserve stderr and stop with the failed append; do not claim durable recording succeeded. The answer must land on this same Card before a fresh Luna executor resumes from the amended record. Self-granted scope cuts or defaults in code comments are forbidden. This protocol also applies to the evidence-triggered stop-and-report paths below.

## Task 1 — Compile-ready domain, errors, listener, and startup foundation

**Owner:** fresh Luna executor 1. **Reads:** approved domain/error/console contracts and amendments; `src/{domain,error,lib,main}.rs`; both console harness modules `http.rs` and `dom.rs`; `Cargo.toml`, crate manifest/lock, `deny.toml`; locally resolved dependency manifests as needed. **Writes:** `src/{domain,error,lib,console}.rs`, crate manifest/lock, focused unit tests in those modules. Do not add `Reader` re-export before it exists or a `serve` placeholder body.

Implement the approved QueueCard composition, MigrationVersion and SchemaIncompatibility payload enum and displays; approved errors/contexts/category mappings, preserving all existing source mappings. `PublishStartup` is Unavailable, displays exactly `startup line could not be published`, and exposes its I/O source. Update category documentation and exhaustive tests for the new variants.

Implement Port, private LoopbackIp, LoopbackAddr, DEFAULT_PORT, Listener, Startup, ConfigSource and its single formatter spelling, and publish_startup. Keep constructors/private fields exactly at the approved visibility. Listener resolves port zero from the bound OS socket and never retries another port. Startup derives its URL from the bound listener. Publish one JSON line and flush the caller's writer; distinguish write and flush failure through the same approved typed error. No intermediate formatted strings when writing directly to a formatter/writer suffices. JSON values/URLs required by the public API are legitimate owned outputs.

**Subtle — test first:** before the listener/startup publication and new error-mapping bodies, write and run red `listener_zero_reports_bound_port`, `occupied_port_returns_bind_error_without_retry`, `startup_uses_bound_listener`, `publish_writes_one_line_and_flushes`, `publish_handles_partial_writes`, `publish_write_failure_preserves_source`, `publish_flush_failure_preserves_source`, and `publish_startup_error_category_and_display`. Run these crate-local cases with `cargo test --locked -p daemar-card --lib <test-name>` and retain each red result before its corresponding body, then green evidence.

Add only dependencies needed by the approved Axum/Askama design: Axum, Askama, Tokio net and any HTTP connection adapter required by Task 3. Installed Axum 0.8.9 and Askama 0.14.0 are candidates, not an instruction to update unrelated dependencies. Resolve explicit minimal features, record actual versions, and retain serde_json arbitrary_precision. Inspect cargo-deny results before accepting additions. A required policy exception or incompatible dependency cannot be hidden by editing deny.toml; escalate with the dependency path. If Task 3 needs another minimal adapter dependency, ownership of manifest/lock transfers explicitly there.

**Acceptance:** `cargo test --locked -p daemar-card --lib`, `cargo clippy --all-targets --all-features -- -D warnings`, `just check` (record expected remaining S3 failures). Tests cover typed address formatting for both families, port zero resolution and occupied-port error, ConfigSource spellings, startup fields agreeing with listener, exactly one newline/flush on success, failing/partial writers and flush errors, category/display/source behavior. Do not require IPv6 binding on an environment without IPv6 support; both address constructors/formatting still require tests and environmental lack must be reported. Conformance emphasis C1–C9, C13–C16. Handoff names actual dependencies and foundation API delivered.

## Task 2 — Verified read-only Reader and shared read queries

**Owner:** fresh Luna executor 2 after Task 1 acceptance. **Reads:** `src/{storage,domain,error,lib}.rs`, `migrations/0001_cards.sql`, approved Reader/schema contract and R4/R8/R9; `tests/features/{console,reading,durability,creation,append,events}.feature`, console schema/snapshot/corruption fixtures in `tests/behavior/console/mod.rs`; installed SQLx 0.9.0 options/migration/pool implementation as needed. **Writes:** `src/storage.rs` and `src/lib.rs`, private storage test module(s) only.

Add Reader and export it. Preserve writer open/migrate/permission and write behavior; Store owns a Reader on its existing pool and delegates list/history so query text and decoders have one implementation. Console Reader open is a separate capability path: check an existing file, no create, no migrations/table creation, no chmod, no writer fallback; read_only(true), create_if_missing(false), journal mode unspecified, immutable false. Pool settings are max/min one, no idle timeout or max lifetime. Reconnection/file replacement remains the explicitly excluded contract boundary; do not invent revalidation or immutable mode.

Read bookkeeping without `ensure_migrations_table` or `Migrator::run`. Compare all applied versions/checksums/success flags against the embedded set in deterministic order: dirty anywhere first, then missing/empty bookkeeping, pairwise version then checksum mismatch, then shorter/longer tails. Keep a private pure comparison seam so synthetic multi-migration sets can prove Behind and first-mismatch precedence without changing migrations. SQL/query failure remains Storage(VerifySchema), not an invented schema reason. Missing files map DatabaseMissing; other opening failures retain the correct storage context.

Queue is one fresh query in cards.rowid order; it reads highest-sequence recorded_at, never payload JSON. Entry lookup constrains both CardId and EntryId; absent Card and absent/foreign entry remain distinct. Reuse existing typed decoders and corrupt/storage classifications. No pool getter, write method, cache, secondary identity list query, or public schema-test helper.

**Test-first acceptance:** add classifier cases for exact, missing table/empty history, dirty precedence over other differences, behind with a nonempty proper prefix, ahead with first surplus version, divergent version, checksum mismatch, multiple competing differences. Exercise real DB opens for refusal without metadata/bytes/mode mutation, current schema success, and successful reads while another live WAL writer appends. Assert write attempts through the Reader's actual private connection fail, while normal reads work. Test queue ignores corrupt payload but rejects required timestamp/card corruption, queue ordering/latest sequence, absent/foreign entry, existing Store reads and writes after delegation. Use scenario-local temp databases only.

Run `cargo test --locked -p daemar-card --lib`, `just check`; preserve all predecessor unit results and all S1/S2 behavior acceptance. Record remaining S3 failures by scenario name, not an invented intermediate count. Conformance emphasis C3/C6/C7/C9–C12/C15/C16. Handoff explicitly identifies the one read capability, query ownership, pool settings, and schema precedence evidence.

## Task 3 — HTTP console, whole-page rendering, and security boundaries

**Owner:** fresh Luna executor 3 after Task 2 acceptance. **Reads:** `src/{console,storage,domain,error}.rs`; approved console/Page/error/attribute contract and all R1–R9; full `tests/features/console.feature`, `tests/behavior/console/{mod,dom,http}.rs`, `browser/tests/{console.spec,console}.ts`; dependency serving/template APIs. **Writes:** `src/console.rs`, private `src/console/` modules, `templates/`, the one `static/console.css`, focused unit tests; manifest/lock only for a specifically documented necessary connection adapter.

Use Axum routing with state containing only Arc<Reader>; put the C12 reason at its introduction. Request processing first rejects a foreign/present-empty Host with empty 421, then non-GET/HEAD methods with 405 on every path, including unknown routes and CSS. Accept only the three approved host spellings with the actual bound port. Build these as outer request guards so unknown-path dispatch cannot bypass them. GET/HEAD serve `/`, `/cards/{card_id}` with optional `entry`, and the stylesheet; unknown paths are 404. HEAD returns status/headers without a body. Infallibly convert the extracted Card path text to CardId, preserving malformed-ID 404 behavior.

**Subtle — test first:** before the guard, accept-loop, and whole-page failure bodies, write and run red `foreign_host_precedes_unsafe_method`, `empty_host_returns_empty_421`, `host_rejection_precedes_unreadable_storage`, `unsafe_method_unknown_route_returns_405`, `unsafe_method_css_returns_405`, `accept_failure_returns_serve_with_source`, `stream_failure_has_queue_without_partial_card`, `queue_failure_has_no_queue_or_card`, and `render_failure_returns_fixed_plain_text_500`. Run each crate-local case with `cargo test --locked -p daemar-card --lib <test-name>` before its corresponding body and retain red/green evidence. Use the private injected accept-operation seam for the accept failure case; a nominal framework result type is not failure evidence.

**Accept-loop implementation constraint:** installed Axum 0.8.9 documents that `axum::serve` never returns an error and retries TCP accept failures. Simply mapping its nominal io::Result would not implement the approved Serve failure. Use a private fallible Tokio accept loop with Axum Router and Hyper/Hyper-util connection serving as needed. Connection tasks implement HTTP service, not prohibited workflow/background polling. Keep connection failure handling separate from listener accept failure. A private injected accept-operation seam must deterministically prove that an accept I/O failure returns Error::Serve with its source. No public generic listener API, no raw HTTP parser, no speculative framework. If this cannot be implemented within the existing public contract, stop with the exact incompatibility for orchestration judgment.

Read queue once per page request; Card identity comes from its matching QueueCard in that request. Read fresh complete history, and membership-checked selected entry. Build a private Page outcome only after all required reads succeed. Missing Card/entry: HTML 404 with readable queue and no partial Card; corrupt/storage stream/entry: HTML 500 with readable queue and no partial Card; queue failure: HTML 500 saying storage failed, no queue or Card data. Match errors explicitly; no fabricated identity, default payload, or partial stream.

Use Askama HTML autoescaping, never `safe`. Match the exact data-role/data-field/id/sequence/aria-current contract from the DOM oracle. Render queue, selected identity, full stream, reported provenance and inspector as visible text. Inspector payload is the producer payload as exposed by history_fields; stage/summary are separate and absent payload means **no payload element**, never null or the storage envelope. Preserve arbitrary-precision JSON numeric spelling. Producer text appears only in text nodes; links/identity markers use typed IDs; datetime attributes use server timestamps. A small private RFC3339 formatter in the library is permitted. Keep all page formatting buffered until successful; Askama failure produces fixed plain-text 500, with no new error variant. One local CSS asset; no JS, inline style, external resources, favicon addition, derived status, or pagination.

**Acceptance before CLI exists:** `cargo test --locked -p daemar-card --lib` with focused private-router/server tests for guard ordering (including foreign Host plus unsafe method), unknown-route unsafe method, HEAD, malformed ID, cross-Card selection, payload absence, whole-page error mapping, fixed render-failure fallback, and injected accept failure. Test Host guard with storage made unreadable to prove rejection precedes reads. Keep existing HTML/HTTP acceptance unchanged; it will run fully after Task 4. Run `just check` and C1–C16 pass; unfinished CLI scenarios must be named as deferred. Handoff lists route/guard/reader/render ownership and any admitted adapter dependencies.

## Task 4 — CLI integration and full acceptance repair

**Owner:** fresh Luna executor 4 after Task 3 acceptance. **Reads:** `src/main.rs`, completed `src/console.rs`/Reader API, full executable console and configuration specs, browser fixtures/config, approved binary flow, predecessor evidence. **Writes:** `src/main.rs` and focused binary unit tests. Any correction in predecessor-owned production files requires an explicit repair packet transferring that file; there is still only one writer.

Add discoverable serve with private clap u16 port parsing and DEFAULT_PORT.get(), converted once to Port. Replace binary ConfigSource/name() with library ConfigSource Display everywhere, keeping source spelling in one implementation. Resolve configuration, open Reader directly (never open_store/prepare_factory_home), bind IPv4 loopback, construct Startup from that listener, publish/flush locked stdout, then serve using **the same listener**. All failures enter Failure::Domain. Ensure the existing generic JSON-success printer does not emit a second line for serve; use a private outcome/control-flow separation if needed. Preserve every existing command's JSON and resolver precedence.

**Subtle — test first:** before the CLI startup sequencing, failure-mapping, and output-control bodies, write and run red binary unit cases `reader_failure_is_domain_without_startup`, `bind_failure_is_domain_without_startup`, `publish_failure_is_domain_and_prevents_serve`, `serve_failure_is_domain`, `serve_uses_published_listener`, and `serve_emits_no_second_success_line`. Run each with `cargo test --locked -p daemar-card --bin card <test-name>` before its corresponding body and preserve red/green evidence; use private injection seams where required to observe failure paths without changing the approved public surface.

Run focused startup/CLI checks including failure before startup, invalid port input, default port declaration, all configuration sources, and failure publishing stdout. Then run `just check`, `just browser`, and `cd browser && ./node_modules/.bin/tsc --noEmit`. The final browser run must build current card via `just browser`; never report a stale binary's success. Browser prerequisites may require the existing `just browser-install` workflow under execution's normal permission policy, but no installation is implied by this planning run. Do not let npx silently substitute a missing dependency/version; verify local packages first.

Capture real network requests/trace on any fetch-set failure. Favicon is an escalation **only after an observed failure with evidence**, per R7; do not preemptively add a route/asset or weaken the exact fetch set. Repair code and CSS against existing visibility/navigation/error oracles; do not modify browser expectations to fit the implementation. All 119 admitted Cucumber scenarios and all 11 admitted browser tests must pass; additions may increase totals, never replace the admitted tests.

## Conformance and orchestrator repair loop

Every task handoff carries a C1–C16 checklist against its entire Rust delta, with location/reason for each finding or a concise checked/no-finding entry. The expensive orchestrator adjudicates conformance **inside execution**, using code and conventions, not a separate independent-review stage. Luna resolves every accepted finding and reruns affected focused tests plus gates; unresolved conventions findings cannot be merely ticketed to cross this operator's stricter all-green barrier.

Checklist: C1 native public errors; C2 manual Error/Display and dependency policy; C3 typed payloads/sources; C4 no production panic/indexing; C5 reasoned expectations/ignores; C6 enums and single parse boundaries; C7 Port/MigrationVersion/IDs/standard units; C8 contextual conversions; C9 approved-only public surface; C10 QueueCard composition and shared queries/types; C11 semantic clones; C12 documented Arc necessity; C13 writer/formatter streaming with justified whole-response buffering; C14 named chosen defaults and sole ConfigSource spelling; C15 no new semantic boolean API; C16 separation of opening/verifying/querying, guarding/routing/rendering, and CLI orchestration. Gates only partially enforce these: a green Clippy run is not the checklist.

Admission/dispatch sequence:

1. Snapshot HEAD, status, full relevant working diff and untracked contents with digests; preserve original user work. Confirm approved contract available and file ownership uncontested. Supply only task-relevant context and explicit expected predecessor API, tests, allowed outputs, commands and stop conditions to a new Luna executor.
2. Executor reports changed files, focused red/green evidence and commands, full gate output, deferred successor scenarios, dependency deltas and C-ID checklist. Orchestrator inspects patch scope and evidence. A task failing its own acceptance stays in execution.
3. On a reproducible code/conventions failure, issue one bounded fresh Luna repair packet: exact symptom/command, relevant contract, allowed files, acceptance and evidence from prior attempt. The expensive orchestrator may diagnose but never writes the repair. Rerun failed check and checks affected by the repair; then full final gates when eligible.
4. On environment failure (advisory lock, socket permission, missing browser/tool, network fetch), preserve stderr and distinguish infrastructure from implementation. Follow the environment's escalation mechanism; no disabled checks, offline substitute presented as full gate, dependency-policy relaxations, or blanket retries. If authority is genuinely missing, report the concrete required action.
5. If the same repair fails twice, pause that dispatch pattern for expensive-model diagnosis and re-scope the next Luna packet from the evidence. This is not acceptance or permission to abandon the task. Scope/API conflicts, conflicting user edits, necessary protected-file changes or dependency-policy exceptions require explicit judgment/authority before mutation.

## Final barrier and durable handoff

After the last repair, freeze the candidate manifest and run **unfiltered** `just check`, `just browser`, and browser TypeScript check on that candidate. Require zero failures/skips/ignored accepted scenarios, all repository gate stages completed, all admitted browser tests green, all conventions findings resolved, no public surface drift and no protected-file weakening. No independent review starts before this barrier. Changing code afterwards invalidates affected evidence and requires appropriate reruns plus the final barrier. Never use `--no-verify` as completion.

The orchestrator's durable evidence packet must include: Card/task identity and supplied authority digest; this plan and each exact dispatched packet; base/candidate HEAD and complete dirty/untracked manifests; implementation patch including new files; actual executor model/provenance and predecessor handoffs; tool/dependency versions and lockfile delta; commands, cwd, exit status and complete relevant logs (including baseline failures and permission outcomes); focused red/green security/schema tests; measured final test totals; browser reports/traces/screenshots where produced; final C1–C16 adjudication with repaired findings and rerun links; API/scope fence check; remaining environmental limitations, if any. Store evidence at paths explicitly supplied to executors; do not depend on chat-only assertions. Root orchestration owns recording this packet onto the Card and delivering the subsequent independent-review input; executors author only the mandated escalation decisions and completion stage-event, never review records.

After the all-green barrier above, the final Luna executor must itself append the completion stage-event to this same Card, with producer `implementer`, kind `agent`. Before this append, confirm `just check` passed, the public API follows the approved typed skeleton, all accepted scope has tests, and the Rust C-ID conformance findings are resolved under this plan's stricter barrier. Replace the summary below with the candidate identity, measured final test totals, and durable evidence path/digest; the event records execution completion, not an independent-review verdict:

```sh
card --db "${PER84_CARD_DB:?dispatch must supply the authoritative Card database}" append 01a0693e-bc16-7272-9ce9-a20f3b07f875 --entry-type stage-event --producer implementer --producer-kind agent --stage execution --summary 'PER-84 implementation complete: identify the all-green candidate, measured test totals, and durable evidence path and digest.'
```

Capture the returned entry ID and sequence in the final handoff. A failed append leaves this completion obligation open; preserve the error and report it. Orchestrator evidence recording does not substitute for this executor-authored event. Independent review remains a subsequent stage after the all-green barrier.

No unresolved product choice currently requires new operator approval: supplied API approval and R1–R9 cover the implementation. Actual protected-test defects, dependency-policy conflicts, favicon fetch failures, API necessity beyond the contract, or unavailable execution permissions are evidence-triggered escalations, not assumed blockers. Chromium execution, the final dependency graph, fallible server integration, and complete final acceptance remain unverified work.
