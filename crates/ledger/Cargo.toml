[package]
name = "ledger"
version = "0.1.0"
edition = "2021"
description = "The append-only ledger and the slip fold. See CONTEXT.md."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
