[package]
name = "daemar-sandbox"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "One-shot sandboxed command execution: VM boundary, no network, overlay filesystem. Behaviors: specs/sandbox.md"

[dependencies]
tar.workspace = true
uuid.workspace = true
thiserror.workspace = true

[dev-dependencies]
